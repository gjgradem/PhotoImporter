<Window x:Class="PhotoImporter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="500" Width="400"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!--First Row-->
        <TextBlock Height="70" Text="1ste stap: Selecteer de opslag waar je de foto's vanaf wilt halen" FontSize="20" TextWrapping="WrapWithOverflow" VerticalAlignment="Top"/>
        <ComboBox Height="30" />
        <!--Second Row-->
        <TextBlock Grid.Row="1" Text="{Binding SampleText}" />
    </Grid>
</Window>
